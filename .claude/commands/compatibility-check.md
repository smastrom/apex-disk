Run a full compatibility verification for ApexDisk against the declared minimum target (macOS 10.15 Catalina, Safari 13).

Follow every step in [`reference/compatibility.md`](../reference/compatibility.md) in order; do not skip any. Surface each step's result inline so the report is auditable.

After completing all checks, update `reference/compatibility.md` with any findings that differ from the current content. If everything checks out, say so.
