---
description: Load the relevant reference file before starting work
---

Before starting any task, read the relevant reference file from `reference/`
based on the operation. Load BEFORE writing or changing code.

**Task routing (match Operation or Aliases):** `reference/index.md`

Never commit code that contradicts a reference file — if a reference is wrong,
update it in the same change.

Unsure what lint-staged vs `/sync` covers? Read `reference/agent-workflow.md`.
